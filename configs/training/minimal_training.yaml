# 训练配置

training:
  id: minimal_training
  algorithm: placeholder
  max_iterations: 10
  seed: 42

environment:
  scene_ref: minimal_scene:0.1.0
  num_envs: 1
  randomization_enabled: true

artifacts:
  output_dir: artifacts/training/minimal_training